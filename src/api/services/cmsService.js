const cmsService = {
  getContent: async () => ({ data: {} }),
  updateContent: async (data) => ({ data }),
  getHowItWorks: async () => ({ data: [] }),
  getExcellenceLeague: async () => ({ data: [] }),
};

export default cmsService;
